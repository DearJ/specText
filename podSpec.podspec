

Pod::Spec.new do |s|

  s.name         =  "podSpec"
  s.version      =  "0.0.1"
  s.summary      =  "firs"
  s.homepage     =  "https://github.com/DearJ/specText.git"
  s.source       =  { :git => "https://github.com/DearJ/specText.git" }
  s.source_files =  "podSpec/**/*.{h,m}"
  s.requires_arc =  true

end
